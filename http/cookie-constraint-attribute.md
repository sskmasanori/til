## Cookie Constraint Attribute

```
Expires, Max-age : when this cookie becomes valid.

Domain : Server which the client send this cookie to.

Path : When the client access this value, send this cookie.

Secure : Send this cookie only when HTTPS access.

HttpOnly: JavaScript can not operate this cookie.

SameSite: None, Lax, Strict.
```